# Mod
mod_name=EasyAuth
mod_id=easyauth
mod_version=3.4.3

# Fabric
minecraft_version=[VERSIONED]
loader_version=0.18.1
fabric_version=[VERSIONED]

# NeoForge (only set on `*-neoforge` nodes)
# https://projects.neoforged.net/neoforged/neoforge
neoforge_version=[VERSIONED]

# Dependencies
# https://github.com/NucleoidMC/Server-Translations/releases
server_translations_version=[VERSIONED]
# https://github.com/lucko/fabric-permissions-api/releases
fabric_permissions_version=[VERSIONED]
luckperms_version=[VERSIONED]
floodgate_api_version=[VERSIONED]
vanish_version=[VERSIONED]

argon2_version=2.12
bcrypt_version=0.10.2
bytes_version=1.6.1
leveldb_version=0.12
mongodb_version=5.3.1
mysql_version=9.2.0
sqlite_version=3.49.1.0
postgresql_version=42.7.10
hocon_version=4.2.0

# Publishing
display_name=[VERSIONED]
supported_versions=[VERSIONED]
supported_minecraft_version=[VERSIONED]

# Other
org.gradle.jvmargs=-Xmx8G
org.gradle.parallel=true