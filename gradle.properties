# Gradle launch arguments must stay here (they can't be set from stonecutter.properties.toml).
# All mod, version and dependency properties live in stonecutter.properties.toml.
org.gradle.jvmargs=-Xmx8G
org.gradle.parallel=true
